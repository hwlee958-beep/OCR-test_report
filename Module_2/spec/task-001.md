# task-001: 스캐폴드 및 UI 구성

- 대상 PRD: `Module_2/docs/prd/ocr-pass-fail-prd.md`
- 원칙: UI-First. UI·라우팅·연동 경로를 먼저 확정 후 판정 로직 착수
- 후속 문서: `task-002.md` (추출·판정·보고서 구현)

## Phase-01 프로젝트 스캐폴드

| ID | 과제 | 상태 |
|---|---|---|
| T-101 | `Module_2/project/frontend` 에 Vite + TypeScript + React 프로젝트 생성 | ❌ |
| T-102 | `Module_2/project/backend` 에 FastAPI 프로젝트 및 가상환경 생성 | ⚠️ 가상환경만 생성 |
| T-103 | `Module_2/project/output` 디렉토리 생성 (보고서 저장 위치) | ❌ |
| T-104 | `.gitignore` 에 가상환경·`node_modules`·`output` 산출물 제외 등록 | ❌ |
| T-105 | Windows용 실행 배치 스크립트 작성 (`run-frontend.bat`, `run-backend.bat`, CRLF) | ❌ |
| T-106 | DB 미사용 확정에 따라 DB 관련 설정·의존성 미포함 확인 | ❌ |

## Phase-02 라이브러리 설치 및 동작 검증

| ID | 과제 | 상태 |
|---|---|---|
| T-201 | BE: `fastapi`, `uvicorn`, `python-multipart` 설치 | ❌ |
| T-202 | BE: OCR 엔진 설치 및 로컬 동작 확인 (RapidOCR/ONNX Runtime 선정) | ✅ |
| T-203 | BE: docx 처리 라이브러리 설치, 서식 파일 열기·저장 왕복 확인 | ❌ |
| T-204 | BE: 이미지 처리 라이브러리 설치 및 png/jpg 로드 확인 | ❌ |
| T-205 | FE: 라우팅 라이브러리 설치 및 기본 라우트 동작 확인 | ❌ |
| T-206 | 샘플 이미지 OCR 텍스트 추출 가능 여부 확인 | ✅ |
| T-207 | 서식 파일의 플레이스홀더 치환 결과가 Word에서 정상 열림 확인 | ❌ |

## Phase-03 화면 및 라우팅 구성

- 구성: 단일 흐름 중심 (업로드 → 검토·보정 → 보고서), 상단 메뉴 사용
- 시각 기준: `.claude/docs/DESIGN.md` 준수

| ID | 과제 | 상태 |
|---|---|---|
| T-301 | 공통 레이아웃 구성 (상단 제목·메뉴, 콘텐츠 최대 폭 유지) | ❌ |
| T-302 | 라우트 `/` 업로드 화면: 다건 파일 선택, 파일 목록, 개별 제외, 실행 버튼 | ❌ |
| T-303 | 라우트 `/review` 검토 화면: 판정 결과 표, 상태별 건수, 상세 패널 | ❌ |
| T-304 | 라우트 `/report` 보고서 화면: 머리말 입력 폼, 생성·다운로드 버튼 | ❌ |
| T-305 | 라우트 `/settings` 설정 화면: Backend 주소, Limit, CF 입력 및 초기화 | ❌ |
| T-306 | 판정 기준 A·B 결과를 각각 표시하는 표 컬럼 구성 | ❌ |
| T-307 | 상태 표시 규칙 구현 (PASS·FAIL·보류를 색상 + 텍스트 동시 표기) | ❌ |
| T-308 | 빈 상태·로딩·오류·성공 안내 문구 배치 | ❌ |
| T-309 | 375px·1280px 화면에서 레이아웃 깨짐·라벨 잘림 확인 | ❌ |
| T-310 | 키보드 이동 및 포커스 표시, 입력 라벨 연결 확인 | ❌ |

## Phase-04 FE·BE 연동 스캐폴드

| ID | 과제 | 상태 |
|---|---|---|
| T-401 | BE: `GET /health` 구현 (`{"status":"ok"}` 반환) | ❌ |
| T-402 | BE: CORS 설정, 개발용 `http://localhost:5173` 허용 | ❌ |
| T-403 | FE: Backend 주소 입력·저장(브라우저 저장), 공백·끝 슬래시 정리, 잘못된 URL 거부 | ❌ |
| T-404 | FE: 주소 우선순위 적용 (저장값 → `VITE_API_BASE_URL` → 미설정 안내) | ❌ |
| T-405 | FE: `BE 연결 확인` 버튼 및 진행 중·성공·실패·제한시간 상태 표시 | ❌ |
| T-406 | 모든 API 요청에 공통 주소 적용, 개발 proxy 의존 금지 | ❌ |
| T-407 | BE 중지 상태·잘못된 주소 입력 시 실패 표시 확인 | ❌ |
| T-408 | BE: 스텁 API `POST /judge`(고정 응답), `POST /report`(빈 docx 반환) 구현 | ❌ |
| T-409 | FE에서 스텁 API 호출로 업로드 → 결과 표 → 보고서 다운로드 전 흐름 관통 확인 | ❌ |

## Phase-05 검증 및 정리

| ID | 과제 | 상태 |
|---|---|---|
| T-501 | FE 빌드 성공 확인 | ❌ |
| T-502 | 배치 스크립트로 FE·BE 각각 기동 확인 | ❌ |
| T-503 | 업로드 → 검토 → 보고서 화면 전환 및 새로고침 동작 확인 | ❌ |
| T-504 | 대용량·다건 파일 선택 시 목록 표시 동작 확인 | ❌ |
| T-505 | 테스트용 프로세스 종료 (Vite 5173, uvicorn 8000) | ❌ |
| T-506 | 확인된 항목·미검증 항목 기록 | ❌ |

## 확인 필요 사항

- OCR 엔진은 RapidOCR(ONNX Runtime)로 확정, 샘플 20장 추출 성공 18/20·오추출 0건
- 서식 플레이스홀더 치환 방식은 T-203·T-207 결과로 확정
